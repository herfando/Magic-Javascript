export function multiply(x, y) { return x * y; }   // multiply two numbers
export function isEven(number) { return number % 2 === 0; } // check even
export function greet(name) { return "Hello, " + name; }   // greeting
